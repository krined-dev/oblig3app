<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layoutUpdatePatient"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".patientList.PatientListFragment">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="text"
            android:layout_marginLeft="20dp"
            android:id="@+id/update_ward_id"
            android:hint="@string/new_ward_hint"
            android:layout_marginRight="20dp"
            android:layout_marginTop="100dp"
            app:layout_constraintTop_toTopOf="parent"
            android:autofillHints="@string/new_ward_hint"/>

        <Button
            android:id="@+id/updatePatient"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/update_ward_id"
            android:layout_marginTop="20dp"
            android:text="@string/update_pat" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>